# ============================================================
# Archivo de ejemplo de variables de entorno (.env.example)
# Copia este archivo a ".env" y reemplaza valores.
# Mantiene compatibilidad con claves existentes y añade las del ref.
# ============================================================

# --- App ---
APP_NAME="Ticket System API"          
APP_ENV="development"                 
LOG_LEVEL="INFO"                      
# Compat
ENVIRONMENT=development               # COMPAT
DEBUG=true                            # Opcional

# --- Red/API ---
API_HOST=0.0.0.0
API_PORT=8000

# --- Seguridad base ---
SECRET_KEY=changeme                   

# --- JWT / Autenticación ---
# Preferidos (nombres de referencia)
JWT_SECRET="CHANGE_ME"
JWT_ALG="HS256"
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Compatibilidad con tus nombres anteriores (usa los de arriba si puedes)
JWT_SECRET_KEY=CHANGE_ME              # COMPAT deprecado de: JWT_SECRET
JWT_ALGORITHM=HS256                   # COMPAT deprecado de: JWT_ALG
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=60    # Igual significado

# --- CORS (separados por coma) ---
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# --- Base de datos ---
# Opción 1: Postgres local 
DATABASE_URL=postgresql+psycopg://appuser:apppass@localhost:5432/tickets

# Opción 2: Postgres genérico (plantilla)
# DATABASE_URL=postgresql+psycopg://USER:PASSWORD@HOST:5432/DBNAME

# Opción 3: SQLite (rápido para dev local; requiere aiosqlite)
# DATABASE_URL=sqlite+aiosqlite:///./dev.db
